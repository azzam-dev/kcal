# PROGRESS — kcal

آخر تحديث: 2026-09-06. حالة حيّة — حدّثها كلما تغيّر شيء فعلي في الكود أو القاعدة،
لا تتركها تجمد. اكتب هذا الملف افتراضاً أن القارئ لا يملك أي سياق عن المحادثات التي أنتجته.

**المشروع:** تطبيق ويب عربي، جوال-أولاً، لتتبع السعرات والعناصر الغذائية الكبرى.
الخطة في [docs/PLAN.md](./docs/PLAN.md)، والقرارات ومبرراتها في
[docs/DECISIONS.md](./docs/DECISIONS.md)، وأعراف الكود في [CLAUDE.md](./CLAUDE.md).

**الحالة: المراحل ٠١ و٠٢ و٠٣ مكتملة. القاعدة الحية عليها الـschema، والتسجيل
مُتحقَّق منه طرفاً لطرف. المتبقي الوحيد: تشغيل اختبارات RLS الاثني عشر
(تحتاج مفتاح `service_role` في `.env.test.local`).**

---

## تم

### التخطيط

- **المواصفات محوَّلة إلى خطة تنفيذ كاملة** (`docs/PLAN.md`): النطاق، المعمارية، محرك
  الحساب بمعادلاته وحدود أمانه، نموذج بيانات من عشرة جداول، طبقة الطعام والبحث العربي،
  الـAPI، بنية المجلدات، الواجهة، الأمان، الاختبارات، الأداء، اثنتا عشرة مرحلة بمعيار
  «مُنجَز حين» لكل واحدة، والمخاطر.

- **القرارات الأربعة الكبرى مثبَّتة:** عربي فقط مع i18n وRTL من اليوم الأول · ويب على
  Next.js جوال-أولاً · بذرة أطعمة داخلية من USDA خلف واجهة `FoodProvider` · بلا موعد
  ثابت، ونقطة الشحن نهاية المرحلة ٠٦.

- **ثمانية تعديلات معتمدة على المواصفات الأصلية.** أهمها: لقطة غذائية في كل صف تسجيل
  (النموذج الأصلي كان يعيد كتابة التاريخ بأثر رجعي)، ومستوى النشاط كمعامل وحيد في الحساب
  (استخدامه مع أيام التدريب ازدواج)، وعمودا منطقة زمنية و`log_date` محلي.

### المرحلة ٠١ — ما بُني فعلاً

- **المستودع:** الفرع `main` على **`azzam-dev/kcal`** (عام)، مدفوع بالكامل،
  و**CI أخضر**. الدفع الأول فشل حتى أُضيف نطاق `workflow` إلى توكن `gh` — بدونه يرفض
  GitHub رفع أي ملف تحت `.github/workflows/`.
- **مشروع Supabase:** أُنشئ في **فرانكفورت `eu-central-1`**، المرجع
  **`xwmnvwveoxgajyzbbsja`**، الحالة `ACTIVE_HEALTHY`، التكلفة صفر (الطبقة المجانية).
  اختيرت فرانكفورت لأن الرياض إليها ~90–110ms، ولأن لـVercel منطقة مقابلة (`fra1`)
  فيصير مسار الاستعلام قفزة واحدة. (المشروع الشقيق في سنغافورة، ~150ms — لم يُكرَّر.)
  `.env.local` مكتوب محلياً بالرابط والمفتاح المنشور، وهو مستثنى من git.
- **السقالة:** Next 16.3.4 + React 19.2.8 + TypeScript 5.9 (`strict` مع
  `noUncheckedIndexedAccess` و`noImplicitOverride` و`noFallthroughCasesInSwitch`) +
  Tailwind v4. السقالة مكتوبة يدوياً لا بـ`create-next-app`، فلا يوجد ملف واحد غير مستخدم.
- **نظام التوكنز** في `@theme` داخل `src/app/globals.css`: أسطح بميل أخضر خفيف (لا رمادي
  محايد)، ثلاثي macros (بروتين/كارب/دهون) مخصص للبيانات لا للزينة، لون accent واحد،
  و`danger`. **لا لون صريح في أي مكوّن.** ثيم داكن واحد عن قصد.
- **بنية i18n:** `src/i18n/ar.ts` قاموس عربي، و`translate()` مع تعويض `{placeholder}`،
  و`getMessages()` لربط اللغة. المفاتيح مُنمَّطة فالمفتاح الناقص خطأ ترجمة.
  `layout.tsx` يقرأ `lang` و`dir` من `DIRECTION`/`DEFAULT_LOCALE`، لا مكتوبَين يدوياً.
- **بدائل الواجهة:** `Button` و`Card` فقط — العدد الذي ترندره الشاشة الوحيدة.
- **صفحة انتظار** بالعربي، زر "ابدأ" فيها **معطَّل عمداً** لأن `/register` غير موجود بعد.
- **الاختبارات:** Vitest، ثمانية اختبارات على طبقة الترجمة، كلها تمر.
- **CI:** GitHub Actions — `typecheck` · `lint` · `test` · `build` · `npm audit`
  (يحجب على critical فقط).
- **سير عمل migrations:** `supabase init` تمّت، و`supabase/migrations/` فيه أول ملف
  (`enable_search_extensions`: `pg_trgm` و`unaccent` في schema `extensions`).
- **`.gitattributes`** لتوحيد نهايات الأسطر (تطوير على ويندوز، CI على أوبونتو).

### أعطال حقيقية اكتُشفت وأُصلحت في المرحلة ٠١

1. **كل نص في التطبيق كان يُعرض بخط النظام الافتراضي بصمت.** صنف `next/font` كان على
   `<body>`، بينما Tailwind يعلن `--font-sans: var(--font-plex-arabic), …` على `:root` —
   فيصبح `--font-sans` غير صالح وقت حساب القيمة عند `:root` ويُورَّث فارغاً.
   **البناء واللينت والأنواع كانت كلها نظيفة**؛ لم يظهر العطل إلا بفحص
   `getComputedStyle(document.body).fontFamily` في المتصفح. الإصلاح: الصنف على `<html>`.
   **مُتحقَّق منه:** الخط المحسوب الآن `"IBM Plex Sans Arabic", …` والأوزان 400/500/600 محمَّلة.
2. **ESLint 10 يسقط اللينت كلياً** مع `eslint-config-next@16.3.4`. التفصيل في
   `docs/DECISIONS.md`. الحل: التثبيت على 9.39.5.
3. **`next.config.ts` رفض مفتاح `eslint`** — أسقطت Next 16 تشغيل اللينتر ضمن البناء.
   الأثر: البناء الناجح لا يعني لينت نظيف، فالبوابة أربعة أوامر لا واحد.

**حالة التحقق:** `npm run typecheck` و`npm run lint` و`npm test` و`npm run build`
كلها نظيفة، والصفحة **فُحصت فعلياً في المتصفح على 375px**.

### المرحلة ٠٢ — المصادقة والملف الشخصي

- **Migration ثانية** (`create_profiles`): جدول `profiles` بأنواع enum للجنس ومستوى
  النشاط والهدف، وقيود `CHECK` للحدود المنطقية، وRLS كاملة، وtrigger `handle_new_user`
  و`set_updated_at`، ودالة `delete_own_account()`.
  **صلاحيات أقل ما يمكن:** لا `INSERT` ولا `DELETE` لأي عميل — الصفوف تأتي من الـtrigger
  وتزول بالـcascade. **مطبَّقة ومُتحقَّق منها على القاعدة الحية** (انظر أدناه).
- **طبقة Supabase:** عميل متصفح، وعميل خادم لكل طلب، ومنطق البوابة في `session.ts`،
  و`src/proxy.ts`. البوابة تستخدم `getUser()` لا `getSession()` — الثانية تثق بالكوكي
  كما هو، والأولى تتحقق منه مع خادم المصادقة.
- **تدفقات المصادقة كاملة:** تسجيل · دخول · خروج · طلب استعادة · تعيين كلمة مرور جديدة ·
  مسار `/auth/confirm` الذي يستبدل رمز الرابط بجلسة · حذف الحساب.
- **صفحة `/profile`:** تعديل اسم العرض، وبطاقة حذف حساب بتأكيد بكتابة كلمة.
- **`lib/errors.ts`:** رمز خطأ Supabase ← مفتاح رسالة، مع تسجيل الأصل في الخادم.
  **لا نص خطأ خام يصل المستخدم في أي مسار.**
- **`lib/redirect.ts`:** `safeRedirectPath` يمنع open redirect عبر `?next=`.
- **`NEXT_PUBLIC_SITE_URL`:** روابط البريد تُبنى منه لا من ترويسة `Host`.
- **٤٢ اختبار وحدة تمر** (ترجمة · تحويل الأخطاء · إعادة التوجيه الآمنة · سكيمات Zod).
- **١٢ اختبار RLS مكتوبة** في `tests/integration/rls/profiles.test.ts` — تتخطّى نفسها
  الآن لغياب الصلاحيات.

**عطل حقيقي كشفه اختبار:** كان البريد يُفحص شكلياً **قبل** إزالة المسافات، فبريد بمسافة
زائدة (لصق، أو لوحة مفاتيح جوال) يُرفض بحجة أنه «غير صحيح» بلا أن يرى المستخدم السبب.
الترتيب صار: تطبيع ثم تحقق.

**حالة التحقق:** typecheck ولينت واختبارات وبناء نظيفة. البناء يؤكد تسجيل
`ƒ Proxy (Middleware)`. **فُحص في المتصفح:** الصفحة العامة و`/login` و`/register`
تُعرض صحيحة على 375px، وطلب `/profile` بلا جلسة **يُحوَّل فعلاً إلى تسجيل الدخول**،
**والتسجيل يعمل طرفاً لطرف مقابل القاعدة الحية** (التفصيل أدناه).

**لم يُجرَّب بعد:** الدخول، وتعديل اسم العرض، وحذف الحساب من الواجهة، واستعادة كلمة
المرور — كلها تحتاج حساباً مؤكَّد البريد، والتأكيد مفعَّل.

### المرحلة ٠٣ — محرك الحساب (مكتملة ومُتحقَّق منها بالكامل)

**لا تعتمد على القاعدة إطلاقاً، فهي المرحلة الوحيدة المُتحقَّق منها تحققاً كاملاً.**

- `src/features/nutrition/domain/` — تايبسكربت نقي: `types` · `bmr` · `tdee` · `goal` ·
  `macros` · `index`. **٥٥ اختباراً** على هذا المجلد وحده.
- **BMR بـMifflin-St Jeor** خلف واجهة `BmrFormula`، **وTDEE** بالمعاملات القياسية،
  **وتعديل الهدف** (‎−٢٠٪ / ٠ / ‎+١٢٪ / معدل مخصص عبر 7700 kcal/kg)،
  **ومحرك macros** خلف واجهة `MacroStrategy` (بروتين أولاً، ثم دهون، والكربوهيدرات الباقي).
- **الوزن المرجعي للبروتين مُقيَّد عند BMI 25** — بدونه يُطلب من شخص وزنه ١٤٠كجم
  ٢٨٠g بروتين يومياً. الاختبار يثبت أن الناتج ١٤٥g.
- **إعادة توزيع الـmacros عند ضيق السعرات:** البروتين والدهون يُسحبان نحو أرضيتيهما
  **بنفس النسبة من الفائض فوق الأرضية**، فلا يُضحَّى بأحدهما لحماية الآخر.
- **كل رقم مُعدَّل يُعاد معه سببه** كرمز في `adjustments`، لا كجملة — الدومين لا يعرف
  لغة المستخدم.
- **`architecture.test.ts` يفرض نقاء الدومين آلياً:** لا استيراد من React أو Next أو
  Supabase أو طبقات التطبيق، ولا `new Date()` إلا كقيمة افتراضية لوسيط.

**نُقض قرار كُتب في الخطة قبل تنفيذه: «لا تنزل السعرات تحت الـBMR أبداً».**
معامل «خامل» 1.20 وخسارة الدهون ×0.80 يعطيان `0.96 × BMR`، أي أن القاعدة كانت
**ستُفعَّل لكل مستخدم خامل** وتعرض عليه تنبيه سلامة مقابل تغيير ٤٪ في الرقم — وتنبيهٌ
يظهر للجميع يعلّم الناس تجاهل التنبيهات. حدّ العجز ٢٥٪ يحصر أسوأ حالة عند `0.90 × BMR`،
والأرضيتان ١٢٠٠/١٥٠٠ تغطيان الأجسام الصغيرة. **اختبار يحرس هذا الحذف** حتى لا يُعاد بحسن نية.

**حالة التحقق:** ١٣١ اختباراً يمر، وtypecheck ولينت وبناء نظيفة.

### تطبيق الـschema والتحقق منه (2026-09-06)

**الـmigrationان مطبَّقتان على المشروع الحي**، و`list_migrations` تُظهر النسختين
`20260906072941` و`20260906074514` **مطابقتَين لاسمَي الملفين في الريبو** — لا انحراف.

**سبب الفشل السابق:** `supabase link` لم يكتمل ولو مرة (ملف `supabase/.temp/project-ref`
لم يكن موجوداً، والمشروعان كلاهما `"linked": false`). لم تكن المشكلة في تسجيل الدخول —
الـCLI كان مسجَّلاً فعلاً ويخزّن توكنه في مدير اعتماد ويندوز لا في ملف.
**ولم تُطبَّق أي migration على المشروع الخطأ:** فُحص `graduationproj-web` فلم يكن فيه أي
كائن من kcal. بعد `link` نجح `db push` **بلا الحاجة إلى كلمة مرور القاعدة** — الـCLI
يهيّئ دور دخول عبر واجهة الإدارة.

**المُتحقَّق منه على القاعدة الحية:**

| | |
|---|---|
| الامتدادات | `pg_trgm` و`unaccent` مثبَّتان |
| RLS على `profiles` | مفعَّلة، سياستان |
| الـtriggers | `on_auth_user_created` و`profiles_set_updated_at` |
| `delete_own_account` | موجودة |
| صلاحيات `anon` | **صفر** |
| صلاحيات `authenticated` | **`SELECT,UPDATE` فقط** |

**تدفق التسجيل مُجرَّب طرفاً لطرف في المتصفح:** ملء النموذج ← إعادة توجيه إلى
`/register/check-email` ← **صف في `auth.users` وصف في `profiles` بنفس الـid**،
و`display_name` **فارغ** (الـtrigger لم يخترع اسماً من مقطع البريد)، و`onboarded_at`
فارغ، والافتراضيات `Asia/Riyadh` و`ar`، والحساب ينتظر تأكيد البريد (التأكيد مفعَّل كما قُرِّر).
ثم حُذف الحساب، **فزال صف `profiles` معه — وهذا يثبت `ON DELETE CASCADE` عملياً.**

**الأنواع مولَّدة:** `src/lib/database.types.ts` موجود، والوسيط النوعي `Database`
ممرَّر في عملاء Supabase الثلاثة، فأي عمود غير موجود صار خطأ ترجمة.

**`src/lib/database-contract.test.ts`** يمنع انحراف أنواع الدومين عن enums القاعدة:
تحقق على مستوى النوع (يفشل البناء) وعلى مستوى التشغيل (يفشل الاختبار لو وُجدت قيمة في
الاثنين بلا معامل في المحرك). بدونه، إضافة قيمة إلى enum في القاعدة تجعل المحرك يقرأ صفاً
بلا معامل ويُنتج `NaN` سعرة بدل خطأ.

---

## قيد العمل

**لا شيء نصف مكتوب في الكود.** المرحلتان ٠١ و٠٢ محجوبتان من جهة القاعدة فقط،
والمرحلة ٠٣ مكتملة.

---

## التالي

### خطوة واحدة متبقية: تشغيل اختبارات RLS

الاثنا عشر اختباراً في `tests/integration/rls/profiles.test.ts` مكتوبة وتتخطّى نفسها
حتى الآن. **هذا أهم اختبار أمني في المشروع ولم يُشغَّل بعد ولو مرة.**

الملف `.env.test.local` مُعَدّ مسبقاً في جذر المشروع (مستثنى من git) وفيه الرابط
والمفتاح المنشور. **ينقصه سطر واحد:** `SUPABASE_SERVICE_ROLE_KEY=`

يُؤخذ من لوحة Supabase ← Project Settings ← API keys ← `service_role` (المُعلَّم "secret")،
ويُلصَق في الملف — **لا في محادثة ولا في أي ملف مُتتبَّع** — ثم `npm.cmd test`.

المفتاح يتجاوز RLS كلياً، ولهذا يُستعمل في الاختبارات **لإنشاء الحسابات المؤقتة وحذفها
فقط**؛ أما السلوك المُختبَر فيمر بجلسة حقيقية عبر المفتاح المنشور كما يفعل المتصفح.
لا ملف تحت `src/` يستورده.

### وخطوة إعداد يدوية في لوحة Supabase

Authentication ← URL Configuration: أضف `http://localhost:3001` (وعنوان Vercel لاحقاً)
إلى Site URL وRedirect URLs. بدونها يفشل رابط تأكيد البريد ورابط إعادة تعيين كلمة المرور.
لا توجد أداة تلمس إعدادات المصادقة، فهي يدوية بالضرورة.

### ثم الربط بـVercel

منطقة الدوال `fra1` لتطابق منطقة القاعدة، والمتغيرات الثلاثة `NEXT_PUBLIC_*` من لوحة
Vercel (تُحقن وقت البناء، فأي تغيير لها يوجب إعادة بناء بلا كاش)، و`NEXT_PUBLIC_SITE_URL`
يصير عنوان الإنتاج.

### ثم المرحلة ٠٤ — الـOnboarding والأهداف

الخطوات الثماني، شاشة النتيجة بالشرح المبسّط، وكتابة أول صف في `nutrition_targets`.
**تعتمد على القاعدة** (`nutrition_targets` لم يُنشأ بعد)، فهي محجوبة حتى تُطبَّق الـmigrations.

---

## مشاكل معروفة

- **اختبارات RLS الاثنا عشر تتخطّى نفسها** لغياب `SUPABASE_SERVICE_ROLE_KEY` في
  `.env.test.local`. التخطّي مقصود ليبقى تشغيل المساهم أخضر، لكنه يعني أن **أهم اختبار
  أمني في المشروع لم يُشغَّل بعد ولو مرة**. انظر «التالي».
- **تدفقات ما بعد الدخول لم تُجرَّب في المتصفح:** الدخول، وتعديل اسم العرض، وحذف الحساب،
  واستعادة كلمة المرور. كلها تحتاج حساباً مؤكَّد البريد، وتأكيد البريد مفعَّل. أقرب طريق
  للتحقق منها هو تسجيل حساب ببريدك الحقيقي بعد ضبط Redirect URLs في لوحة Supabase.
- **`supabase db push` يعمل بلا كلمة مرور القاعدة** — الـCLI يهيّئ دور دخول عبر واجهة
  الإدارة. هذا يعني أن إعادة تعيين كلمة مرور القاعدة **ليست شرطاً** لسير العمل،
  خلافاً لما كان مكتوباً هنا سابقاً. الشرط الفعلي هو `supabase link` وحده.
- **ESLint مثبَّت على 9.39.5 وnpm يحذّر أنه غير مدعوم.** مقصود؛ الترقية إلى 10 تسقط
  اللينت. يُعاد النظر حين يدعم `eslint-config-next` الإصدار 10.
- **TypeScript مثبَّت على 5.9 بينما 7.0 صادر.** الترقية مهمة مستقلة بتحققها الخاص.
- **لا اختبارات مكوّنات ولا طرف-لطرف بعد.** Playwright وTesting Library يُضافان في
  مرحلتيهما (٠٦ و١٠)، غير مثبَّتَين.
- **الاسم المعروض للمنتج غير محدد.** `kcal` اسم المجلد والمستودع فقط؛ الاسم الظاهر نص
  في `src/i18n/ar.ts` يُغيَّر في أي لحظة.
- **أرقام الواجهة لاتينية (1، 12) لا عربية-هندية (١، ١٢).** مقصود ومطابق للسائد في
  تطبيقات المنطقة؛ لو أردت العكس فهو تغيير في طبقة التنسيق لا في كل نص.
