# PROGRESS — NUTRIVA

آخر تحديث: 2026-09-06. حالة حيّة — حدّثها كلما تغيّر شيء فعلي في الكود أو القاعدة،
لا تتركها تجمد. اكتب هذا الملف افتراضاً أن القارئ لا يملك أي سياق عن المحادثات التي أنتجته.

**المشروع:** تطبيق ويب عربي، جوال-أولاً، لتتبع السعرات والعناصر الغذائية الكبرى.
الخطة الكاملة في [docs/PLAN.md](./docs/PLAN.md)، والقرارات ومبرراتها في
[docs/DECISIONS.md](./docs/DECISIONS.md)، وأعراف الكود في [CLAUDE.md](./CLAUDE.md).

**الحالة: المراحل ٠١ و٠٢ و٠٣ من ١٢ مكتملة، والمرحلة ٠٤ نصفها منجز.**
البوابة الرباعية خضراء (`typecheck` · `lint` · `test` · `build`)، ١٣٥ اختبار وحدة يمر،
و٢٢ اختبار RLS مكتوبة **لم تُشغَّل بعد** (تنتظر مفتاحاً — انظر «مشاكل معروفة»).
المتبقي من المرحلة ٠٤: **شاشات Onboarding، وaction الحفظ، وإعادة توجيه غير المُهيَّأ.**

**الهوية:** الاسم الظاهر `NUTRIVA`، وشعاره `your daily nutrition, made simple.` بالإنجليزية
كعلامة لا كنص واجهة. **كل نصوص الواجهة بعامية سعودية بيضاء** — القاعدة واستثناءاتها
الثلاثة مكتوبة في رأس `src/i18n/ar.ts`، **اقرأها قبل إضافة أي نص.**

**المصادقة:** **تأكيد البريد مطفأ** في لوحة Supabase، فالتسجيل يدخّل المستخدم التطبيق
مباشرة. (مؤقت ومشروط بالإرجاع — انظر «مشاكل معروفة».)

**المشروع الحي:** Supabase ref `xwmnvwveoxgajyzbbsja` (فرانكفورت `eu-central-1`).
**المستودع:** `azzam-dev/kcal` — اسم المجلد والمستودع لم يتغيّرا، `NUTRIVA` اسم ظاهر فقط.
الفرع الإنتاجي `main`. **لم يُنشر على Vercel بعد، والنطاق المقصود `nutriva.app`.**

**حالة git وقت الكتابة:** `main` المحلي متقدّم على `origin/main` بكوميتين، **وعمل اليوم
الأخير (الهوية واللهجة وحذف تأكيد البريد) غير مُلتزَم بعد.**

---

## تم

### المرحلة ٠١ — الأساس

- **السقالة مكتوبة يدوياً** لا بـ`create-next-app`: Next 16.3.4 + React 19.2.8 +
  TypeScript 5.9 (`strict` مع `noUncheckedIndexedAccess`) + Tailwind v4. لا ملف واحد غير مستخدم.
- **نظام توكنز** في `@theme` داخل `src/app/globals.css`: ثيم داكن واحد، أسطح بميل أخضر
  خفيف، ثلاثي macros (بروتين/كارب/دهون) مخصص للبيانات لا للزينة. **لا لون صريح في أي مكوّن.**
- **بنية i18n وRTL من الكوميت الأول:** قاموس عربي بمفاتيح مُنمَّطة، وتعويض `{placeholder}`،
  و`layout.tsx` يقرأ `lang`/`dir` من `src/i18n` لا مكتوبَين يدوياً.
- **سير عمل migrations في الريبو** (`supabase/migrations/`) — الدَّين الذي وقع فيه المشروع
  الشقيق `graduationproj-web` مُغلق من البداية.
- **CI على GitHub Actions:** `typecheck` · `lint` · `test` · `build` · `npm audit`
  (يحجب على critical فقط). قيم `NEXT_PUBLIC_*` في CI **placeholders مقصودة** فلا تلمس
  الوظيفةُ القاعدةَ الحية، وناتجها لا يُنشر.
- **`.gitattributes`** لتوحيد نهايات الأسطر (تطوير على ويندوز، CI على أوبونتو).

### المرحلة ٠٢ — المصادقة والملف الشخصي

- **Migration `create_profiles`** مطبَّقة: جدول `profiles`، وأنواع enum
  (`gender` · `activity_level` · `goal`)، وقيود `CHECK`، وRLS، وtrigger `handle_new_user`
  و`set_updated_at`، ودالة `delete_own_account()`.
- **تدفقات كاملة:** تسجيل · دخول · خروج · طلب استعادة كلمة مرور · تعيين كلمة مرور جديدة ·
  حذف الحساب. و`/auth/confirm` يستبدل رمز رابط البريد بجلسة، ولم يبقَ من يستعمله إلا
  استعادة كلمة المرور بعد إطفاء تأكيد البريد.
- **التسجيل يدخّل المستخدم مباشرة**، بلا شاشة «افحص بريدك» ولا رابط تأكيد. بُنيت شاشة
  التأكيد وزر إعادة الإرسال ثم **حُذفا في نفس اليوم بقرار صريح** — تاريخ git يحفظهما،
  وإرجاع التأكيد استرجاعٌ لهما لا كتابة من جديد.
- **صفحة `/profile`:** تعديل اسم العرض، وبطاقة حذف حساب بتأكيد بكتابة كلمة.
- **`lib/errors.ts`:** رمز خطأ Supabase ← مفتاح رسالة، والأصل يُسجَّل في الخادم.
  **لا نص خطأ خام يصل المستخدم في أي مسار.**
- **`lib/redirect.ts`:** `safeRedirectPath` يمنع open redirect عبر `?next=`.
- **`src/proxy.ts`:** بوابة الجلسة (اصطلاح Next 16). تستخدم `getUser()` لا `getSession()`.

### المرحلة ٠٣ — محرك الحساب

- **`src/features/nutrition/domain/`** — تايبسكربت نقي: `types` · `bmr` · `tdee` · `goal` ·
  `macros` · `index`. BMR بـMifflin-St Jeor خلف واجهة `BmrFormula`، وTDEE بالمعاملات
  القياسية، وتعديل الهدف (‎−٢٠٪ / ٠ / ‎+١٢٪ / معدل مخصص عبر 7700 kcal/kg)، ومحرك macros
  خلف واجهة `MacroStrategy`.
- **`architecture.test.ts` يفرض النقاء آلياً:** لا استيراد من React أو Next أو Supabase أو
  طبقات التطبيق، ولا `new Date()` إلا كقيمة افتراضية لوسيط.

### المرحلة ٠٤ — المنجز منها: الطبقة الخلفية كاملة

- **رسائل المحرك:** `src/i18n/ar.ts` صار فيه رسالة لكل قيمة في `AdjustmentReason`
  (مساحة `adjustment.`) وفي `InputIssue` (مساحة `field.`)، و**`src/features/nutrition/messages.ts`**
  يحوّل الرمز إلى مفتاح عبر `adjustmentKeys()` و`inputIssueKey()`.
  **لا شيء يرندر هذه الرسائل بعد** — أول مستهلك لها هو شاشة النتيجة في «التالي».
- **جدول `nutrition_targets`** (`supabase/migrations/20260906152058_create_nutrition_targets.sql`)
  مطبَّق على القاعدة الحية ومتحقَّق منه بالاستعلام: RLS مفعَّلة، سياستان فقط
  (`select` و`insert`)، صلاحيات `authenticated` = `SELECT,INSERT` و`anon` صفر، وكل قيود
  `CHECK` موجودة. **جدول إلحاقي: لا `update` ولا `delete` — لا سياسة ولا صلاحية.**
- **`src/server/targets.ts`:** `getCurrentTarget(today)` و`insertTarget(row)` — استعلامات
  فقط، بلا قواعد عمل وبلا ساعة.
- **اختبارات RLS للجدول** (`tests/integration/rls/nutrition-targets.test.ts`، عشرة اختبارات)
  تغطي العزل بين مستخدمين، ورفض صف موجَّه لغير صاحبه (`42501`)، ورفض `update`/`delete`
  من المالك نفسه، وصفاً واحداً لكل يوم (`23505`)، وقيود `CHECK`، **ومعيار «مُنجَز» للمرحلة**:
  هدف بتاريخ لاحق يُضاف بجانب السابق ولا يمسّه. وتجهيز الحسابات مشترك في
  `tests/integration/actors.ts`. **لم تُشغَّل بعد.**

### الهوية والسجل اللغوي وقابلية التثبيت

- **الاسم `NUTRIVA`** في `app.name`، وشعاره الإنجليزي في `app.tagline` يُرندَر في الصفحة
  الرئيسية داخل `<bdi lang="en" dir="ltr">`. **وصف الصفحة والـmanifest يقرآن `home.body`**
  (العربي) لا الشعار.
- **~١١٠ نصوص حُوِّلت إلى عامية سعودية بيضاء** بثلاثة استثناءات مكتوبة في رأس `ar.ts`.
- **`src/app/manifest.ts` و`icon.svg` و`apple-icon.tsx`** — مولَّدة من الكود، **ولا مجلد
  `public/` في المشروع**. التطبيق يُثبَّت على الجوال بأيقونة ونافذة مستقلة.
  **ولا يعمل دون اتصال** (لا service worker — خارج نطاق الـMVP).
- **`src/lib/brand.ts`** يحمل اللونين الوحيدين اللذين يعيشان خارج CSS (`canvas` و`accent`)،
  لأن المتصفح ونظام التشغيل لا يقرآن custom property.

### التحقق على القاعدة الحية

`supabase migration list` يُظهر الثلاث (`20260906072941` · `20260906074514` ·
`20260906152058`) **مطابقةً لأسماء الملفات في الريبو**، فلا انحراف بين الريبو والقاعدة.

| المُتحقَّق منه | النتيجة |
|---|---|
| الامتدادات | `pg_trgm` و`unaccent` مثبَّتان |
| RLS على `profiles` | مفعَّلة، سياستان · صلاحيات `authenticated` = `SELECT,UPDATE` |
| RLS على `nutrition_targets` | مفعَّلة، سياستان · صلاحيات `authenticated` = `SELECT,INSERT` |
| صلاحيات `anon` على الجدولين | **صفر** |
| الـtriggers | `on_auth_user_created` · `profiles_set_updated_at` |
| `delete_own_account` | موجودة ومُجرَّبة من الواجهة |
| تأكيد البريد | مطفأ (`mailer_autoconfirm: true`) |

**التسجيل والحذف مُجرَّبان طرفاً لطرف في المتصفح:** ملء النموذج ← **دخول مباشر إلى
`/profile`** بلا شاشة وسيطة ← صف في `auth.users` **مؤكَّد** وصف في `profiles` **بنفس الـid**،
و`display_name` فارغ (الـtrigger لا يخترع اسماً من مقطع البريد)، و`onboarded_at` فارغ،
والافتراضيات `Asia/Riyadh` و`ar`. ثم حُذف الحساب **من بطاقة الحذف في الواجهة نفسها**
فزال صف `profiles` معه — **وهذا يثبت `delete_own_account()` و`ON DELETE CASCADE` عملياً.**

**الأنواع مولَّدة:** `src/lib/database.types.ts` والوسيط النوعي `Database` ممرَّر في عملاء
Supabase الثلاثة، فأي عمود غير موجود صار خطأ ترجمة.

### أعطال حقيقية اكتُشفت وأُصلحت

1. **كل نص في التطبيق كان يُعرض بخط النظام الافتراضي بصمت.** صنف `next/font` كان على
   `<body>` بينما Tailwind يعلن `--font-sans` على `:root`، فصار غير صالح وقت حساب القيمة
   ووُرِّث فارغاً. **البناء واللينت والأنواع كانت الثلاثة نظيفة**؛ لم يظهر إلا بفحص
   `getComputedStyle(document.body).fontFamily` في المتصفح. الصنف الآن على `<html>`.
2. **ESLint 10 يسقط اللينت كلياً** مع `eslint-config-next@16.3.4`: إضافته الانتقالية
   `eslint-plugin-react` تستدعي `context.getFilename()` المحذوف في 10.
3. **`next build` في Next 16 لم يعد يشغّل ESLint** — البناء الناجح لا يعني لينت نظيف،
   ولهذا البوابة أربعة أوامر لا واحد.
4. **التحقق من البريد كان يفحص الصيغة قبل إزالة المسافات** — بريد بمسافة زائدة يُرفض
   بحجة أنه غير صحيح بلا أن يرى المستخدم السبب. **كشفه اختبار فشل.** الترتيب الآن:
   تطبيع ثم تحقق.
5. **بوابة الجلسة كانت تخدم صفحة تسجيل الدخول بدل الـmanifest.** `matcher` في
   `src/proxy.ts` يستثني `.svg` و`.png` ولا يستثني `.webmanifest` ولا `/apple-icon`
   (بلا امتداد أصلاً)، فأي جهاز يطلبهما بلا جلسة — وهو ما يفعله كل جهاز — كان يستلم HTML
   صفحة الدخول. **التطبيق يصير غير قابل للتثبيت بلا أن يظهر خطأ في أي مكان**، والبوابة
   الرباعية خضراء عليه. كُشف بفتح `/manifest.webmanifest` في المتصفح.
6. **`icon.svg` كان لا يُحلَّل أصلاً** — تعليق داخله يذكر أسماء التوكنز، وفيها شرطتان
   متتاليتان، وهي محرّمة داخل تعليق XML، فيردّ المتصفح بصفحة خطأ تحليل بدل الأيقونة.
7. **كل رابط تأكيد بريد كان ينتهي بـ«الرابط انتهت صلاحيته» وهو رابط سليم.**
   `/auth/confirm` كان يفهم شكلاً واحداً من شكلَي رابط Supabase (`token_hash` + `type`)،
   وهو ما ينتجه قالب بريد معدَّل. أما القالب الافتراضي فيمرّ أولاً بـ`/auth/v1/verify`
   عند Supabase، الذي **يستهلك الرمز بنجاح** ثم يعيد التوجيه إلينا بـ`?code=`.
   **الدليل من سجلات Supabase:** `GET /verify` بحالة `303` في نفس اللحظة التي يرى فيها
   المستخدم رسالة انتهاء الصلاحية. الروت الآن يقبل الشكلين ويسجّل أيهما فشل ولماذا.
8. **`email_address_invalid` لم يكن مربوطاً برسالة.** Supabase يرفض عناوين لا يقبلها
   (نطاق بلا سجل MX مثلاً) بعد أن تكون سكيما Zod قبلت الصيغة، فكان المستخدم يرى
   «ما ضبطت العملية» عن الحقل الوحيد الذي يقدر يصلحه. صار مربوطاً بـ`field.emailInvalid`.

---

## قرارات تقنية ومبرراتها

التفصيل الكامل في [docs/DECISIONS.md](./docs/DECISIONS.md). المهم هنا، وكل قرار مع
**البديل الذي رُفض**:

### الهوية واللغة

- **الاسم لاتيني وشعاره إنجليزي، والوصف عربي.** `app.name` + `app.tagline` قفلة علامة
  واحدة تُرندَر في الصفحة الرئيسية. **البديل المرفوض:** ترجمة الشعار — يفقده كونه علامة
  ويكرر ما تقوله الصفحة أصلاً. **والمرفوض أيضاً:** جعل الشعار الإنجليزي وصفَ الصفحة —
  يجعل أول سطر في نتيجة البحث لجمهور عربي إنجليزياً؛ ولهذا الوصف يقرأ `home.body`.
- **عامية سعودية بيضاء لا فصحى**، مع ثلاثة استثناءات: رسائل الأخطاء وأمان الحساب تبقى
  قصيرة صريحة (العامية تطيل الجملة، والخطأ الطويل خطأ متجاوَز)، ومصطلحات التغذية كما هي،
  وإخلاء المسؤولية الطبي جاد. **البديل المرفوض:** فصحى مبسّطة — تبقى نبرة نشرة أخبار.
  **والمرفوض:** لهجة نجدية ضيقة — تخسر القارئ الخليجي بلا مقابل.
- **حدود الأرقام مكتوبة داخل الجمل** (`بين ٩٠ و٢٥٠ سم`) لا `{placeholders}` من ثوابت
  الدومين، مطابقةً لبقية نصوص الملف. **والثمن المعروف:** تغيير الثابت يترك الجملة تكذب،
  ولهذا **لم تُذكر نسبتا سقف العجز والزيادة في رسائلهما أصلاً** — هما أكثر الثوابت عرضة
  للضبط.
- **`manifest` والأيقونات مولَّدة من الكود.** **البديل المرفوض:** ملفات ثابتة في `public/` —
  نسخة ثانية من الاسم والوصف تحمل الاسم القديم بعد أول إعادة تسمية.

### المصادقة

- **تأكيد البريد مطفأ مؤقتاً، وأُزيل كل ما كان يخدمه** (شاشة `check-email`، زر إعادة
  الإرسال، الـaction، الكوكي، رسائلها). **البديل المرفوض:** إبقاؤها معطّلة في الكود —
  شاشة لا يصلها أحد ومسار لا يُسلَك أسوأ من غيابهما، وتاريخ git يحفظهما.
- **بقي حارس واحد في `signUp`:** إن لم ترجع جلسة يعرض «فعّل حسابك أول» بدل إعادة توجيه
  ترتدّ من بوابة الجلسة إلى `/login` بلا سبب ظاهر، ويسجّل سطراً في الخادم.
  **البديل المرفوض:** حذفه أيضاً — يجعل إعادة تشغيل المفتاح تكسر التسجيل بصمت.
- **`/auth/confirm` يقبل شكلَي الرابط** (`?code=` و`?token_hash=`). **البديل المرفوض:**
  الاعتماد على شكل واحد — هو بالضبط ما جعل كل رابط سليم يُقرأ منتهياً.
  **قيد معروف:** مسار الـ`code` يشترط فتح الرابط في نفس المتصفح الذي بدأ العملية
  (مُتحقِّق PKCE في كوكي عنده)؛ قالب `token_hash` لا يشترط ذلك.
- **حذف الحساب عبر دالة `SECURITY DEFINER`.** **البديل المرفوض:** استيراد `service_role`
  في server action — مفتاح يتجاوز RLS كلياً داخل شيفرة التطبيق مقابل عملية واحدة.
- **`NEXT_PUBLIC_SITE_URL` صريح ولا يُشتق من ترويسة `Host`.** **البديل المرفوض:** بناء
  رابط البريد من ترويسة الطلب — يتحكم بها المهاجم، وتسميمها يحوّل بريد إعادة التعيين
  إلى رمز يُسلَّم لغيره.

### محرك الحساب

- **نُقض قرار كان في الخطة قبل تنفيذه: «لا تنزل السعرات تحت الـBMR أبداً».** معامل «خامل»
  1.20 وخسارة الدهون ×0.80 يعطيان `0.96 × BMR`، أي أن القاعدة **كانت ستُفعَّل لكل مستخدم
  خامل** وتعرض تنبيه سلامة مقابل تغيير ٤٪ — وتنبيهٌ يراه الجميع يعلّم الناس تجاهل
  التنبيهات. **المطبَّق بدلاً منها:** سقف عجز ٢٥٪ وأرضية مطلقة ١٥٠٠/١٢٠٠،
  **واختبار في `goal.test.ts` يحرس الحذف** حتى لا يُعاد بحسن نية.
- **الوزن المرجعي للبروتين مُقيَّد عند BMI 25.** **البديل المرفوض:** الوزن الحالي مباشرة —
  يطلب من شخص وزنه ١٤٠كجم ٢٨٠g بروتين يومياً. **والمرفوض:** سقف مطلق ثابت لا يتناسب مع الطول.
- **أنواع الدومين معرَّفة محلياً**، و`src/lib/database-contract.test.ts` يحرس التماس.
  **البديل المرفوض:** استيرادها من `database.types.ts` — يربط المنطق النقي بSupabase.
  بدون الحارس، إضافة قيمة إلى enum في القاعدة تجعل المحرك **يُنتج `NaN` سعرة بدل خطأ**.
- **النقاء مفروض باختبار لا بتعليق.** **البديل المرفوض:** تعليق «لا تستورد React هنا» —
  يصمد حتى أول مرة يكون فيها أحدهم مستعجلاً.
- **`features/nutrition/messages.ts` خارج `domain/`**، ومحوّلاته `Record` **كامل** لا
  `Partial` كالذي في `lib/validation/messages.ts`. ذاك يفهرس رموز Zod المفتوحة فيحتاج
  بديلاً، وهذان يفهرسان اتحادين مغلقين — **فقيمة تُضاف بلا رسالة خطأُ ترجمة**، لا رمز خام
  على الشاشة. وهو خارج `domain/` لأنه يحتاج `MessageKey` والمحرك يجب ألا يحتاجه.

### جدول الأهداف وطبقة الاستعلام

- **`nutrition_targets` إلحاقي بالمنع لا بالاصطلاح:** لا صلاحية `update`/`delete` ولا سياسة
  لهما. **السبب:** يوم أمس قِيس على هدف الأمس، وتعديل الصف يعيد كتابة تاريخ ما إذا كان
  اليوم قد تحقق.
- **`source` نوع enum لا نص بـ`CHECK`** — ليأتي اتحاداً مُنمَّطاً في `database.types.ts`
  فيصير الخطأ المطبعي خطأ ترجمة. لا يخص المحرك في شيء، فلا حارس له في اختبار العقد.
- **`effective_from` بلا `default current_date`.** **السبب:** الافتراضي يكون اليوم بتوقيت
  UTC، و«اليوم» محلي للمستخدم؛ المستدعي هو من يعرف المنطقة الزمنية فهو من يقولها.
- **لا فهرس ثانٍ لاستعلام «أحدث صف حتى اليوم»** — قيد `UNIQUE(user_id, effective_from)`
  يبني الـbtree أصلاً، وPostgres يمسحه عكسياً.
- **`getCurrentTarget(today)` يأخذ اليوم وسيطاً** ولا يقرأ الساعة. **السبب نفسه:** اشتقاقه
  من ساعة الخادم يضع كل من هو خارج UTC على اليوم الخطأ جزءاً من كل يوم.
- **`adjustments` لا تُخزَّن مع الصف.** **السبب:** هي دالة من `inputs` المخزَّنة، وعمودٌ لها
  نسخة ثانية حرّة أن تخالف ما يقوله المحرك اليوم.
- **الكتابة تضع `user_id` والقراءة لا تفلتر بالملكية.** سياسة الـ`insert` تفحص العمود،
  فوضعه تعريفُ الصف بنفسه لا حمايته؛ والقراءة تحميها RLS وحدها. **لا تُضف
  `where user_id = …` كأنه هو الحماية.**
- **تجهيز حسابات الاختبار أُخرج إلى `tests/integration/actors.ts`** عند الاستعمال الثاني،
  وعملاؤه **بلا أنواع عمداً**: اختبار أمني يجب أن يقدر يرسل حمولة تمنعها أنواع التطبيق
  (صف موجَّه لمستخدم آخر) ليرى رفض Postgres لا خطأ الترجمة.

### قرارات بيئة وتشغيل

- **قيم CI هي placeholders.** **البديل المرفوض:** القيم الحقيقية في ملف الـworkflow —
  الـplaceholders تُبقي الوظيفة معزولة: لا تصل القاعدة الحية وناتجها لا يُنشر.
- **ثيم داكن واحد بلا وضع فاتح.** **البديل المرفوض:** ثيم فاتح إلى جانبه — نصف مبني أسوأ
  من غيابه، وهو بالضبط ما حدث في `graduationproj-web` حتى حُذفت متغيراته الميتة.

---

## قيد العمل

**لا شيء نصف مكتوب.** كل ما في الشجرة مكتمل ومارّ على البوابة الرباعية، لكن **عمل اليوم
الأخير غير مُلتزَم في git**: الهوية واللهجة (`src/i18n/ar.ts` · `src/app/page.tsx` ·
`src/app/layout.tsx` · `manifest.ts` · `icon.svg` · `apple-icon.tsx` · `src/lib/brand.ts`)،
وحذف تأكيد البريد (`src/app/(auth)/actions.ts` · حذف `register/check-email/` ·
`src/lib/supabase/session.ts` · `src/app/auth/confirm/route.ts` · `src/lib/errors.ts` ·
`src/proxy.ts`)، وتحديث الوثائق الخمس. **أول خطوة لمن يكمل: التزم هذا وادفعه.**

**نقطة التوقف بالضبط:** المرحلة ٠٤ بلغت نهاية الطبقة الخلفية. الموجود:
`nutrition_targets` في القاعدة، و`src/server/targets.ts`، و`features/nutrition/messages.ts`.
**غير الموجود إطلاقاً:**

- **لا يوجد** `src/app/(onboarding)/`
- **لا يوجد** `src/lib/validation/onboarding.ts`
- **لا يوجد** أي مفتاح `onboarding.*` في `src/i18n/ar.ts`
- **لا شيء يستدعي** `calculateTargets()` ولا `insertTarget()` ولا `adjustmentKeys()` —
  كلها مكتوبة ومختبَرة ولا مستهلك لها بعد.

---

## التالي

### المرحلة ٠٤ — ما تبقّى: شاشات Onboarding وحفظ الهدف

**لا شيء يحجبها. الخطوات أدناه قابلة للتنفيذ كما هي.**

**١. سكيما التحقق — `src/lib/validation/onboarding.ts`**

سكيما Zod واحدة تغطي مدخلات المحرك: `goal` · `gender` · `birthYear` · `heightCm` ·
`weightKg` · `targetWeightKg?` · `activityLevel` · `trainingDays`. **رسائل السكيما رموز
قصيرة** (`height_out_of_range`) لا نصوص، كما في `src/lib/validation/auth.ts`، ويترجمها
`issueKey`. الحدود الحقيقية يفرضها `validateNutritionInput` في المحرك؛ سكيما Zod هنا
للشكل والنوع، لا لإعادة تعريف الحدود.

**٢. الشاشة — `src/app/(onboarding)/onboarding/page.tsx` + مكوّن عميل للخطوات**

مسار **واحد** فيه ثماني خطوات في حالة React، لا ثمانية مسارات: الحفظ يقع مرة واحدة في
النهاية، فمسار لكل خطوة يعني حالة نصف محفوظة بلا فائدة. الترتيب:
الهدف ← الجنس ← سنة الميلاد ← الطول ← الوزن الحالي ← الوزن المستهدف (لو احتاجه الهدف) ←
مستوى النشاط ← أيام التدريب ← شاشة النتيجة.

- الصفحة **تفحص `getUser()` بنفسها** وتعيد التوجيه إلى `/login` عند غيابه، كبقية الصفحات
  المحمية، رغم أن `proxy.ts` يحرسها. حزام إضافي مقصود.
- **الوزن الحالي لا يُكتب في `profiles`** — لا عمود له هناك عمداً، وموضعه `weight_entries`
  في المرحلة ٠٨. حتى تُبنى: مرّره إلى المحرك دون تخزين، **وسجّل ذلك في هذا الملف**.
- **أيام التدريب تُخزَّن في `profiles.training_days` ولا تدخل الحساب.** استخدم
  `activityLevelDisagrees()` من `features/nutrition/domain/tdee.ts` لعرض اقتراح لطيف عند
  التناقض، والاختيار يبقى للمستخدم.
- شاشة النتيجة تعرض مخرجات `calculateTargets()` وتشرحها بلغة بسيطة، وتعرض كل سبب في
  `targets.adjustments` عبر `adjustmentKeys()`، **مع إخلاء مسؤولية أن الأرقام تقديرية
  وليست نصيحة طبية** (نصه يبقى جاداً لا عامياً — انظر رأس `ar.ts`).

**٣. مفاتيح i18n:** أضف مساحة `onboarding.*` لكل نص في الشاشات الثماني وشاشة النتيجة.
لا نص عربي داخل مكوّن.

**٤. الـaction — `completeOnboarding` في `src/app/(onboarding)/actions.ts`**

بالترتيب: تحقق Zod ← `calculateTargets()` ← لو `ok: false` أرجِع
`{ errorKey: inputIssueKey(issues) }` ← وإلا اكتب `profiles` (الجنس، سنة الميلاد، الطول،
مستوى النشاط، أيام التدريب، الهدف، الوزن المستهدف، `onboarded_at = now()`) ثم
`insertTarget()` بـ`source: "calculated"` و`inputs` لقطةَ المدخلات.
ترجع `{ errorKey?, noticeKey? }` كبقية الـactions، وتنتهي بـ`redirect("/profile")`.

**`effective_from` = اليوم بمنطقة المستخدم**، تُقرأ من `profiles.timezone` (افتراضها
`Asia/Riyadh`) وتُحسب بـ`Intl.DateTimeFormat("en-CA", { timeZone })` التي تعطي `YYYY-MM-DD`
مباشرة. **لا تستعمل `new Date().toISOString().slice(0,10)`** — تلك تعطي يوم UTC.

**٥. إعادة توجيه غير المُهيَّأ:** كل صفحة داخل `(app)` تقرأ `getOwnProfile()` أصلاً؛
إن كان `onboardedAt` فارغاً ← `redirect("/onboarding")`. طبّقها في `/profile` الآن،
وفي كل صفحة تُضاف لاحقاً.

**معيار «مُنجَز»:** مستخدم جديد يصل من التسجيل إلى هدف يومي محفوظ في `nutrition_targets`،
واختبار يثبت أن تعديل الهدف لاحقاً يكتب صفاً جديداً ولا يمسّ صفاً سابقاً (الاختبار مكتوب
فعلاً في `tests/integration/rls/nutrition-targets.test.ts` وينتظر تشغيلاً).

### خطوات تحتاج صاحب الحساب (لا تحجب المرحلة ٠٤)

1. **تشغيل اختبارات RLS.** الملف `.env.test.local` مُعَدّ في جذر المشروع (مستثنى من git)
   وفيه الرابط والمفتاح المنشور، **وينقصه سطر واحد**: `SUPABASE_SERVICE_ROLE_KEY=`
   من لوحة Supabase ← Project Settings ← API keys ← `service_role`. ثم `npm.cmd test`.
2. **لوحة Supabase ← Authentication ← URL Configuration:** أضف `http://localhost:3001`
   (و`nutriva.app` لاحقاً) إلى Site URL وRedirect URLs. لازمة **لرابط إعادة تعيين كلمة
   المرور**، وهو المسار الوحيد الباقي الذي يرسل بريداً.
3. **النشر على Vercel:** منطقة الدوال `fra1` لتطابق منطقة القاعدة، والمتغيرات الثلاثة
   `NEXT_PUBLIC_*` من لوحة Vercel، و`NEXT_PUBLIC_SITE_URL` يصير عنوان الإنتاج.
   (تُحقن وقت البناء، فأي تغيير لها يوجب إعادة بناء بلا كاش.)
4. **عند إعادة تفعيل تأكيد البريد** (مطلوب قبل أول مستخدم حقيقي، أي مع بند النشر لا بعده):
   - لوحة Supabase ← Authentication ← Sign In / Providers ← Email ← Confirm email = on،
     **ثم Save داخل البطاقة نفسها** — المفتاح وحده لا يحفظ، وهذه الخطوة ضاعت مرتين من قبل.
   - استرجع شاشة `register/check-email` وزر إعادة الإرسال من تاريخ git (`git log -- "src/app/(auth)/register/check-email"` — الكوميت `840e9da`).
   - عدّل قالب البريد (Authentication ← Emails ← Confirm signup) ليصير الرابط
     `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email`.
   - **امسح حسابات التجربة**: قلب المفتاح لا يمس الحسابات القائمة، وحساب أُنشئ بلا تأكيد
     يبقى غير مؤكَّد ويرفض الدخول إلى الأبد.

---

## مشاكل معروفة

- **اختبارات RLS الاثنان والعشرون لم تُشغَّل ولو مرة.** تتخطّى نفسها لغياب
  `SUPABASE_SERVICE_ROLE_KEY`. التخطّي مقصود ليبقى تشغيل المساهم أخضر، لكنه يعني أن
  **أهم اختبار أمني في المشروع بلا تشغيل فعلي بعد**. الصلاحيات تُحقِّق منها بالاستعلام،
  أما السياسات فلا يثبتها إلا تشغيل الاختبارات بمستخدمَين حقيقيَّين.
- **تأكيد البريد مطفأ.** كل حساب في القاعدة **بريده غير مُثبَت**، فلا يُعتمد عليه ولا
  تُبنى عليه رسالة. مقبول ما دام لا مستخدم حقيقي، **ويُعاد تفعيله مع النشر لا بعده**.
- **خدمة البريد المدمجة في Supabase محدودة بشدة** — بضع رسائل في الساعة للمشروع كله.
  لم تعد تمس التسجيل، **لكنها تحكم استعادة كلمة المرور**. أي تجربة جدية لها تحتاج
  **SMTP مخصصاً** (Resend أو ما شابه).
- **استعادة كلمة المرور لم تُجرَّب في المتصفح**، وتحتاج ضبط Redirect URLs أولاً.
- **تعديل اسم العرض لم يُجرَّب في جلسة مُتحكَّم بها**، لكن صفاً في `profiles` يحمل اسماً
  حُفظ من الواجهة، وهو دليل عملي كافٍ على أن المسار يعمل.
- **لا وضع دون اتصال ولا service worker.** الـmanifest موجود فالتطبيق يُثبَّت ويفتح بنافذة
  مستقلة وله أيقونة، **لكنه لا يعمل بلا شبكة**، ونافذة التثبيت التلقائية في أندرويد تطلب
  service worker. مقصود: الوضع دون اتصال مرفوض في الـMVP.
- **أيقونة iOS مولَّدة بالكود لا بتصميم.** شكل هندسي بلونَي العلامة، يُستبدَل كاملاً حين
  يوجد تصميم فعلي.
- **ESLint مثبَّت على 9.39.5 وnpm يحذّر أنه غير مدعوم.** مقصود؛ الترقية إلى 10 تسقط اللينت
  كلياً. يُعاد النظر حين يدعم `eslint-config-next` الإصدار 10.
- **TypeScript مثبَّت على 5.9 بينما 7.0 صادر** (النقل إلى Go). الترقية مهمة مستقلة بتحققها
  الخاص، لا أثر جانبي لتحديث تبعيات.
- **لا اختبارات مكوّنات ولا طرف-لطرف.** Playwright وTesting Library يُضافان في مرحلتيهما
  (٠٦ و١٠)، وغير مثبَّتَين.
- **أرقام الواجهة لاتينية (1، 12) لا عربية-هندية (١، ١٢).** مقصود ومطابق للسائد في تطبيقات
  المنطقة؛ تغييره يمس طبقة التنسيق لا كل نص. (نثر القاموس يستعمل الهندية: `٨ حروف`.)

### ملاحظات بيئة تُوفّر وقتاً

- **PowerShell على هذا الجهاز يمنع `npm.ps1` و`npx.ps1`** (سياسة `Restricted`).
  استعمل `npm.cmd` و`npx.cmd`، أو Git Bash. التفصيل في README.
- **`supabase db push` لا يحتاج كلمة مرور القاعدة** — الـCLI يهيّئ دور دخول عبر واجهة
  الإدارة. الشرط الفعلي هو `supabase link` وحده، ويُتحقَّق من نجاحه بوجود الملف
  `supabase/.temp/project-ref`. **نسيان `link` هو سبب فشل صامت سابق** استغرق وقتاً لتشخيصه.
- **حذف صفحة تحت `src/app/` يترك أنواعاً مولَّدة قديمة** في `.next/dev/types` تكسر
  `npm run typecheck` بخطأ عن ملف غير موجود. الحل: `rm -rf .next/dev` (أو إعادة تشغيل
  خادم التطوير)، لا تعديل الكود.
- **حالة إعدادات المصادقة تُقرأ بلا لوحة:**
  `GET {SUPABASE_URL}/auth/v1/settings` بترويسة `apikey` يعيد `mailer_autoconfirm`
  (`true` = التأكيد مطفأ). مفيد للتأكد من أن تغييراً في اللوحة حُفظ فعلاً.
