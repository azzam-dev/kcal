# PROGRESS — kcal

آخر تحديث: 2026-09-06. حالة حيّة — حدّثها كلما تغيّر شيء فعلي في الكود أو القاعدة،
لا تتركها تجمد. اكتب هذا الملف افتراضاً أن القارئ لا يملك أي سياق عن المحادثات التي أنتجته.

**المشروع:** تطبيق ويب عربي، جوال-أولاً، لتتبع السعرات والعناصر الغذائية الكبرى.
الخطة الكاملة في [docs/PLAN.md](./docs/PLAN.md)، والقرارات ومبرراتها في
[docs/DECISIONS.md](./docs/DECISIONS.md)، وأعراف الكود في [CLAUDE.md](./CLAUDE.md).

**الحالة: المراحل ٠١ و٠٢ و٠٣ من ١٢ مكتملة ومدفوعة.** الشجرة نظيفة، CI أخضر،
١٣٥ اختبار وحدة يمر، و١٢ اختبار RLS مكتوبة تنتظر مفتاحاً لتشغيلها.
المرحلة التالية هي ٠٤ (Onboarding والأهداف)، **ولا شيء يحجبها**.

**المشروع الحي:** Supabase ref `xwmnvwveoxgajyzbbsja` (فرانكفورت `eu-central-1`).
**المستودع:** `azzam-dev/kcal`، الفرع الإنتاجي `main`. **لم يُنشر على Vercel بعد.**

---

## تم

### المرحلة ٠١ — الأساس

- **السقالة مكتوبة يدوياً** لا بـ`create-next-app`: Next 16.3.4 + React 19.2.8 +
  TypeScript 5.9 (`strict` مع `noUncheckedIndexedAccess`) + Tailwind v4. لا ملف واحد غير مستخدم.
- **نظام توكنز** في `@theme` داخل `src/app/globals.css`: ثيم داكن واحد، أسطح بميل أخضر
  خفيف، ثلاثي macros (بروتين/كارب/دهون) مخصص للبيانات لا للزينة. **لا لون صريح في أي مكوّن.**
- **بنية i18n وRTL من الكوميت الأول:** قاموس عربي بمفاتيح مُنمَّطة، وتعويض `{placeholder}`،
  و`layout.tsx` يقرأ `lang`/`dir` من `src/i18n` لا مكتوبَين يدوياً.
- **سير عمل migrations في الريبو** (`supabase/migrations/`) — الدَّين الذي وقع فيه المشروع
  الشقيق `graduationproj-web` مُغلق من البداية.
- **CI على GitHub Actions:** `typecheck` · `lint` · `test` · `build` · `npm audit`
  (يحجب على critical فقط). قيم `NEXT_PUBLIC_*` في CI **placeholders مقصودة** فلا تلمس
  الوظيفةُ القاعدةَ الحية، وناتجها لا يُنشر.
- **`.gitattributes`** لتوحيد نهايات الأسطر (تطوير على ويندوز، CI على أوبونتو).

### المرحلة ٠٢ — المصادقة والملف الشخصي

- **Migration `create_profiles`** مطبَّقة: جدول `profiles`، وأنواع enum
  (`gender` · `activity_level` · `goal`)، وقيود `CHECK`، وRLS، وtrigger `handle_new_user`
  و`set_updated_at`، ودالة `delete_own_account()`.
- **تدفقات كاملة:** تسجيل · دخول · خروج · طلب استعادة كلمة مرور · تعيين كلمة مرور جديدة ·
  `/auth/confirm` الذي يستبدل رمز رابط البريد بجلسة · حذف الحساب.
- **صفحة `/profile`:** تعديل اسم العرض، وبطاقة حذف حساب بتأكيد بكتابة كلمة.
- **`lib/errors.ts`:** رمز خطأ Supabase ← مفتاح رسالة، والأصل يُسجَّل في الخادم.
  **لا نص خطأ خام يصل المستخدم في أي مسار.**
- **`lib/redirect.ts`:** `safeRedirectPath` يمنع open redirect عبر `?next=`.
- **`src/proxy.ts`:** بوابة الجلسة (اصطلاح Next 16). تستخدم `getUser()` لا `getSession()`.

### المرحلة ٠٣ — محرك الحساب

- **`src/features/nutrition/domain/`** — تايبسكربت نقي: `types` · `bmr` · `tdee` · `goal` ·
  `macros` · `index`. BMR بـMifflin-St Jeor خلف واجهة `BmrFormula`، وTDEE بالمعاملات
  القياسية، وتعديل الهدف (‎−٢٠٪ / ٠ / ‎+١٢٪ / معدل مخصص عبر 7700 kcal/kg)، ومحرك macros
  خلف واجهة `MacroStrategy`.
- **`architecture.test.ts` يفرض النقاء آلياً:** لا استيراد من React أو Next أو Supabase أو
  طبقات التطبيق، ولا `new Date()` إلا كقيمة افتراضية لوسيط.

### تطبيق الـschema والتحقق منه على القاعدة الحية

`list_migrations` تُظهر `20260906072941` و`20260906074514` — **مطابقتين لاسمَي الملفين في
الريبو**، فلا انحراف بين الريبو والقاعدة.

| المُتحقَّق منه | النتيجة |
|---|---|
| الامتدادات | `pg_trgm` و`unaccent` مثبَّتان |
| RLS على `profiles` | مفعَّلة، سياستان |
| الـtriggers | `on_auth_user_created` · `profiles_set_updated_at` |
| `delete_own_account` | موجودة |
| صلاحيات `anon` | **صفر** |
| صلاحيات `authenticated` | **`SELECT,UPDATE` فقط** |

**تدفق التسجيل مُجرَّب طرفاً لطرف في المتصفح:** ملء النموذج ← إعادة توجيه إلى
`/register/check-email` ← صف في `auth.users` وصف في `profiles` **بنفس الـid**،
و`display_name` فارغ (الـtrigger لا يخترع اسماً من مقطع البريد)، و`onboarded_at` فارغ،
والافتراضيات `Asia/Riyadh` و`ar`. ثم حُذف الحساب فزال صف `profiles` معه —
**وهذا يثبت `ON DELETE CASCADE` عملياً.**

**الأنواع مولَّدة:** `src/lib/database.types.ts` موجود، والوسيط النوعي `Database` ممرَّر في
عملاء Supabase الثلاثة، فأي عمود غير موجود صار خطأ ترجمة.

### أعطال حقيقية اكتُشفت وأُصلحت

1. **كل نص في التطبيق كان يُعرض بخط النظام الافتراضي بصمت.** صنف `next/font` كان على
   `<body>` بينما Tailwind يعلن `--font-sans` على `:root`، فصار غير صالح وقت حساب القيمة
   ووُرِّث فارغاً. **البناء واللينت والأنواع كانت الثلاثة نظيفة**؛ لم يظهر إلا بفحص
   `getComputedStyle(document.body).fontFamily` في المتصفح. الصنف الآن على `<html>`.
2. **ESLint 10 يسقط اللينت كلياً** مع `eslint-config-next@16.3.4`: إضافته الانتقالية
   `eslint-plugin-react` تستدعي `context.getFilename()` المحذوف في 10.
3. **`next build` في Next 16 لم يعد يشغّل ESLint** — البناء الناجح لا يعني لينت نظيف،
   ولهذا البوابة أربعة أوامر لا واحد.
4. **التحقق من البريد كان يفحص الصيغة قبل إزالة المسافات** — بريد بمسافة زائدة (لصق أو
   لوحة مفاتيح جوال) يُرفض بحجة أنه غير صحيح، بلا أن يرى المستخدم السبب. **كشفه اختبار فشل.**
   الترتيب الآن: تطبيع ثم تحقق.

---

## قرارات اليوم (2026-09-06)

التفصيل الكامل لكل قرار في [docs/DECISIONS.md](./docs/DECISIONS.md). المهم هنا:

### **نُقض** قرار كان مكتوباً في الخطة قبل تنفيذه: «لا تنزل السعرات تحت الـBMR أبداً»

معامل «خامل» 1.20 وخسارة الدهون ×0.80 يعطيان `0.96 × BMR`، أي أن القاعدة **كانت ستُفعَّل
لكل مستخدم خامل** وتعرض تنبيه سلامة مقابل تغيير ٤٪ في الرقم — وتنبيهٌ يراه الجميع يعلّم
الناس تجاهل التنبيهات. **البديل المطبَّق:** سقف عجز ٢٥٪ (يحصر أسوأ حالة عند `0.90 × BMR`)
وأرضية مطلقة ١٥٠٠/١٢٠٠ تغطي الأجسام الصغيرة. **اختبار في `goal.test.ts` يحرس الحذف**
حتى لا يُعاد بحسن نية.

### الوزن المرجعي للبروتين مُقيَّد عند BMI 25

**البديل المرفوض:** الحساب على الوزن الحالي مباشرة — يطلب من شخص وزنه ١٤٠كجم ٢٨٠g بروتين
يومياً. الأنسجة التي تحتاج تغذية هي الكتلة الرخوة، وهي لا ترتفع بموازاة الدهن.
**والمرفوض أيضاً:** سقف مطلق ثابت — رقم اعتباطي لا يتناسب مع الطول.

### حذف الحساب عبر دالة `SECURITY DEFINER` لا عبر مفتاح `service_role`

**البديل المرفوض:** استيراد `service_role` في server action — يضع مفتاحاً يتجاوز RLS كلياً
داخل شيفرة التطبيق مقابل عملية واحدة. الدالة لا تحذف إلا `auth.uid()`.

### `NEXT_PUBLIC_SITE_URL` صريح، ولا يُشتق من ترويسة `Host`

**البديل المرفوض:** بناء رابط البريد من ترويسة الطلب. الترويسة يتحكم بها المهاجم:
طلب إعادة تعيين بـ`Host: evil.test` يجعل الضحية تستقبل بريداً حقيقياً من الخدمة الحقيقية
يحوي رابطاً يسلّم رمزها للمهاجم.

### أنواع الدومين مُعرَّفة محلياً، مع اختبار عقد يحرس التماس

`Gender` و`ActivityLevel` و`Goal` معرَّفة في `features/nutrition/domain/types.ts`.
**البديل المرفوض:** استيرادها من `database.types.ts` — يربط طبقة المنطق النقي بSupabase
فيسقط سبب وجودها. **والثمن:** إمكان انحراف التعريفين. يغطيه
`src/lib/database-contract.test.ts`: تحقق على مستوى النوع يكسر البناء، وعلى مستوى التشغيل
يكسر الاختبار. بدونه، إضافة قيمة إلى enum في القاعدة تجعل المحرك يقرأ صفاً بلا معامل
**ويُنتج `NaN` سعرة بدل خطأ**.

### نقاء الدومين مفروض باختبار لا بتعليق

**البديل المرفوض:** تعليق يقول «لا تستورد React هنا» — يصمد حتى أول مرة يكون فيها أحدهم
مستعجلاً.

### قيم CI هي placeholders لا قيم المشروع الحقيقية

**البديل المرفوض:** وضع الرابط والمفتاح الحقيقيين في ملف الـworkflow. الـplaceholders تُبقي
الوظيفة معزولة: لا تصل القاعدة الحية، وناتجها لا يُنشر (Vercel يبني بقيمه الخاصة).

### ثيم داكن واحد بلا وضع فاتح

**البديل المرفوض:** ثيم فاتح إلى جانبه — نصف مبني أسوأ من غيابه، وهو بالضبط ما حدث في
`graduationproj-web` حتى حُذفت متغيراته الميتة. كل لون توكن، فإضافة ثيم لاحقاً = إعادة
تعريف كتلة `@theme` وحدها.

---

## قيد العمل

**لا شيء نصف مكتوب. الشجرة نظيفة وكل شيء مدفوع** (آخر كوميت `a0ab41d`).

آخر ما أُنجز كان تطبيق الـmigrations وتوليد `src/lib/database.types.ts` وربطه بالعملاء
الثلاثة، ثم إضافة `src/lib/database-contract.test.ts`. كلها مكتملة ومُختبَرة.

**البند التالي لم يبدأ.** موقعه بالضبط:

- **لا يوجد** `supabase/migrations/*_create_nutrition_targets.sql`
- **لا يوجد** `src/server/targets.ts`
- **لا يوجد** `src/app/(onboarding)/`
- **`src/i18n/ar.ts` ينقصه** مفاتيح رسائل لكل قيمة في `AdjustmentReason` و`InputIssue`
  (المعرَّفتان في `src/features/nutrition/domain/types.ts`). المحرك يُنتج هذه الرموز اليوم
  **ولا يوجد أي نص يعرضها للمستخدم** — وهذه أول فجوة يجب سدّها في المرحلة ٠٤.

---

## التالي

### المرحلة ٠٤ — Onboarding والأهداف (لا شيء يحجبها، ابدأ مباشرة)

**١. Migration جديدة:**

```bash
npx supabase migration new create_nutrition_targets
```

جدول `public.nutrition_targets` **إلحاقي لا يُحدَّث أبداً** (تعديل الهدف يكتب صفاً جديداً
بتاريخ سريان جديد، فلا تتغير الأيام السابقة):

`id` · `user_id` (FK `auth.users` مع `on delete cascade`) · `effective_from date` ·
`bmr` · `tdee` · `calorie_target` · `protein_g` · `carb_g` · `fat_g` ·
`source` (`'calculated'|'custom'`) · `inputs jsonb` (لقطة المدخلات) · `created_at`.

- `UNIQUE(user_id, effective_from)`
- RLS: `select` و`insert` للمالك فقط عبر `(select auth.uid()) = user_id`.
  **لا `update` ولا `delete`** — هذا ما يجعل الجدول إلحاقياً فعلاً لا اصطلاحاً.
- صلاحيات: `revoke all ... from anon, authenticated;` ثم `grant select, insert to authenticated;`

ثم `npm.cmd run db:push` و`npm.cmd run db:types`.

**٢. `src/server/targets.ts`** — استعلامات فقط، بلا قواعد عمل:
`getCurrentTarget()` (الصف الأحدث بـ`effective_from <= today`) و`insertTarget(row)`.

**٣. `src/app/(onboarding)/onboarding/`** — الخطوات الثماني بترتيب المواصفات:
الهدف ← الجنس ← سنة الميلاد ← الطول ← الوزن الحالي ← الوزن المستهدف (لو احتاجه الهدف) ←
مستوى النشاط ← أيام التدريب ← شاشة النتيجة.

- **الوزن الحالي لا يُكتب في `profiles`** — لا عمود له هناك عمداً. يُكتب في
  `weight_entries` (المرحلة ٠٨). حتى تُبنى، مرّره إلى المحرك دون تخزين وسجّل ذلك هنا.
- **أيام التدريب تُخزَّن ولا تدخل الحساب.** استخدم `activityLevelDisagrees()` من
  `features/nutrition/domain/tdee.ts` لعرض اقتراح لطيف عند التناقض، والاختيار يبقى للمستخدم.
- شاشة النتيجة تعرض مخرجات `calculateTargets()` وتشرحها بلغة بسيطة، **مع إخلاء مسؤولية
  أن الأرقام تقديرية وليست نصيحة طبية**.

**٤. `completeOnboarding` server action:** تحقق Zod ← `calculateTargets()` ←
كتابة `profiles` و`nutrition_targets` ← `onboarded_at = now()`.
ترجع `{ errorKey?, noticeKey? }` كبقية الـactions.

**٥. مفاتيح i18n الناقصة:** أضف إلى `src/i18n/ar.ts` رسالة لكل قيمة في `AdjustmentReason`
(`deficit_capped` · `surplus_capped` · `calorie_floor_applied` · `macros_rebalanced` ·
`macros_below_floor` · `target_weight_underweight`) وفي `InputIssue`، ثم دالة تحويل
كالموجودة في `lib/validation/messages.ts`. **الصياغة خالية من اللوم** — قرار موثَّق.

**٦. أعِد توجيه المستخدم غير المُهيَّأ:** صفحة محمية و`onboarded_at` فارغ ← `/onboarding`.

**معيار «مُنجَز»:** مستخدم جديد يصل من التسجيل إلى هدف يومي محفوظ في `nutrition_targets`،
واختبار يثبت أن تعديل الهدف لاحقاً يكتب صفاً جديداً ولا يمسّ صفاً سابقاً.

### خطوات تحتاج صاحب الحساب (لا تحجب المرحلة ٠٤)

1. **تشغيل اختبارات RLS.** الملف `.env.test.local` مُعَدّ في جذر المشروع (مستثنى من git)
   وفيه الرابط والمفتاح المنشور، **وينقصه سطر واحد**: `SUPABASE_SERVICE_ROLE_KEY=`
   من لوحة Supabase ← Project Settings ← API keys ← `service_role`. ثم `npm.cmd test`.
2. **لوحة Supabase ← Authentication ← URL Configuration:** أضف `http://localhost:3001`
   (وعنوان Vercel لاحقاً) إلى Site URL وRedirect URLs. بدونها يفشل رابط تأكيد البريد
   ورابط إعادة التعيين، ولا يمكن تجربة الدخول ولا حذف الحساب من الواجهة.
3. **النشر على Vercel:** منطقة الدوال `fra1` لتطابق منطقة القاعدة، والمتغيرات الثلاثة
   `NEXT_PUBLIC_*` من لوحة Vercel، و`NEXT_PUBLIC_SITE_URL` يصير عنوان الإنتاج.
   (تُحقن وقت البناء، فأي تغيير لها يوجب إعادة بناء بلا كاش.)

---

## مشاكل معروفة

- **اختبارات RLS الاثنا عشر لم تُشغَّل ولو مرة.** تتخطّى نفسها لغياب
  `SUPABASE_SERVICE_ROLE_KEY`. التخطّي مقصود ليبقى تشغيل المساهم أخضر، لكنه يعني أن
  **أهم اختبار أمني في المشروع بلا تشغيل فعلي بعد**.
- **تدفقات ما بعد الدخول لم تُجرَّب في المتصفح:** الدخول، وتعديل اسم العرض، وحذف الحساب،
  واستعادة كلمة المرور. كلها تحتاج حساباً مؤكَّد البريد، والتأكيد مفعَّل — فتحتاج ضبط
  Redirect URLs أولاً.
- **ESLint مثبَّت على 9.39.5 وnpm يحذّر أنه غير مدعوم.** مقصود؛ الترقية إلى 10 تسقط اللينت
  كلياً. يُعاد النظر حين يدعم `eslint-config-next` الإصدار 10.
- **TypeScript مثبَّت على 5.9 بينما 7.0 صادر** (النقل إلى Go). الترقية مهمة مستقلة بتحققها
  الخاص، لا أثر جانبي لتحديث تبعيات.
- **لا اختبارات مكوّنات ولا طرف-لطرف.** Playwright وTesting Library يُضافان في مرحلتيهما
  (٠٦ و١٠)، وغير مثبَّتَين.
- **الاسم المعروض للمنتج غير محدد.** `kcal` اسم المجلد والمستودع فقط؛ الاسم الظاهر نص في
  `src/i18n/ar.ts` يُغيَّر في أي لحظة.
- **أرقام الواجهة لاتينية (1، 12) لا عربية-هندية (١، ١٢).** مقصود ومطابق للسائد في تطبيقات
  المنطقة؛ تغييره يمس طبقة التنسيق لا كل نص.

### ملاحظات بيئة تُوفّر وقتاً

- **PowerShell على هذا الجهاز يمنع `npm.ps1` و`npx.ps1`** (سياسة `Restricted`).
  استعمل `npm.cmd` و`npx.cmd`، أو Git Bash. التفصيل في README.
- **`supabase db push` لا يحتاج كلمة مرور القاعدة** — الـCLI يهيّئ دور دخول عبر واجهة
  الإدارة. الشرط الفعلي هو `supabase link` وحده، ويُتحقَّق من نجاحه بوجود الملف
  `supabase/.temp/project-ref`. **نسيان `link` هو سبب فشل صامت سابق** استغرق وقتاً لتشخيصه.
